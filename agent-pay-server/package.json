{
  "name": "agent-pay-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.2",
    "axios": "^0.21.4",
    "twilio": "^3.73.0"
  },
  "engines": {
    "node": "14"
  }
}